// Pure-patch module — no runtime QML. The patch in module.json swaps the
// bar's leftCenterGroup back to a conditional width that collapses to its
// natural size when no media is playing.
import QtQuick
QtObject {}
